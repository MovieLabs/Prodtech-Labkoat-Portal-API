#Dev environment
.idea
.env
.clinic

# dependencies
build/
dist/
node_modules/

# misc
.env.local
.env.development.local
.env.test.local
.env.production.local


# Local pipeline uploads, used instead of S3 when PIPELINE_LOCAL_ROOT is set
.pipeline-local/

# Captured generator output, for diffing a refactor against. Local only.
.golden/

# Reports, exports and one-off scripts written for a person to read rather than for the build.
# In the repo so they can be found, out of git because they are output, not source.
.scratchpad/
